# AutoGLM UI - Python 3.11 统一环境依赖（Web 版本）
# 整合项目 UI + scrcpy 投屏 + AutoGLM 两种模式

# ==================== Web 框架 ====================
Flask>=3.0.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
python-socketio>=5.11.0
eventlet>=0.33.0

# ==================== ADB 和投屏 ====================
adbutils>=2.0.0
# scrcpy 客户端 - 从 GitHub 安装（PyPI 版本在 3.11 有依赖冲突）
# 安装命令：pip install git+https://github.com/leng-yue/py-scrcpy-client.git

# ==================== 图像处理 ====================
numpy>=1.21.0,<2.0.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
av>=12.0.0

# ==================== 网络和工具 ====================
PyYAML>=6.0
requests>=2.31.0
loguru>=0.7.0

# ==================== AI 模型 ====================
# GLM-4 API
zhipuai>=0.10.0
# OpenAI API（AutoGLM 依赖）
openai>=2.9.0

# ==================== 其他依赖 ====================
sniffio>=1.3.0

# ==================== 可选依赖（根据需要取消注释） ====================
# transformers>=5.0.0rc0  # 本地模型部署
# sglang>=0.5.6.post1     # 模型推理引擎
# vllm>=0.12.0            # 模型推理引擎

# 安装说明：
# 1. 基础依赖：pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# 2. scrcpy 客户端：pip install git+https://github.com/leng-yue/py-scrcpy-client.git
# 3. 如需本地模型部署，取消注释相应行并重新安装
